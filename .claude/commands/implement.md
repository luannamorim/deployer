---
description: Implement a phase from the project briefing
argument-hint: [phase-name]
---

Read docs/BRIEFING.md and implement $ARGUMENTS.

Follow these rules strictly:
1. Identify all commits listed under that phase
2. Implement each commit one at a time, in order
3. After each commit's code is ready, run `make lint` and `make test`
4. Only commit if both pass — fix issues before committing
5. Use the exact commit message format from the briefing (Conventional Commits)
6. Never skip a commit or combine multiple commits into one
7. After completing all commits in the phase, show a summary of what was done

Before starting, use context7 to check the latest docs for any library you are about to use.
